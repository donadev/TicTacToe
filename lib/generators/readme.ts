import { Matrix } from "../data/matrix";
import { Move } from "../data/moves";



export const refreshReadme = (matrix : Matrix, moves : Move[], date : string, ended : boolean, winningSymbol : string | null, path : string) => {
}
